\begin{animation}[id="test-ff"]
\shape{a}{Array}{size=8, data=[8,3,7,1,5,2,6,4]}

\compute{
def iterate(scene, rng):
    arr = scene["a"]
    i = rng.randint(0, len(arr)-1)
    j = rng.randint(0, len(arr)-1)
    if (i < j and arr[i] > arr[j]) or (i > j and arr[i] < arr[j]):
        arr[i], arr[j] = arr[j], arr[i]
    scene["a"] = arr
    return scene
}

\step
\narrate{Mảng ban đầu: [8, 3, 7, 1, 5, 2, 6, 4].}

\fastforward{50}{sample_every=10, seed=42}

\step
\narrate{Hoàn tất. Mảng đã sắp xếp: [1, 2, 3, 4, 5, 6, 7, 8].}
\end{animation}
