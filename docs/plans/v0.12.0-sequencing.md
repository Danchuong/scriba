# v0.12.0 Wave Sequencing Plan

**Status:** Ready for execution · **Author:** architect · **Date:** 2026-04-22
**Branch:** `feat/smart-label-v2.0.0`
**Ruleset target:** v2.1.0-rc (W3 completion)

---

## Versioning — Resolving v0.11.0 / v0.12.0 Ambiguity

Pending tasks #15–#19 carry a stale `v0.11.0` label.

| What happened | Implication |
|---------------|-------------|
| v0.11.0 shipped W3 (R-01, R-07, R-08, R-11–R-16, R-19, R-22, R-25, R-27) on 2026-04-22 | v0.11.0 is closed |
| #15–#19 describe W0/W1/W2/W3 scoring/obstacle work | This is v0.12.0 scope |
| Branch `feat/smart-label-v2.0.0` | Correct — ruleset v2.x, product v0.12.0 |

Tasks #15–#19 are re-labelled v0.12.0 in this document. The task system itself
is not edited here — that is the main agent's responsibility. All commit messages
and CHANGELOG entries on this branch MUST reference `v0.12.0`.

---

## Wave Map

```
W0 (Prep) ──┬── [A] Protocol stubs        non-byte-breaking, parallelisable
            ├── [B] Segment helpers        non-byte-breaking, parallelisable
            ├── [C] Scoring arch doc       this deliverable, non-code
            └── [D] Corpus 3→44            human-gated, parallel to A/B/C

            Gate: W0-[A,B,C] merged

W1 (Scoring) ── Swap boolean → score      byte-breaking: re-pin 3 (→ 44) goldens

            Gate: W1 merged

W2 (AABB obstacles) ── resolve_obstacle_boxes    byte-breaking

            Gate: W2 merged

W3 (Segment obstacles, R-31) ── resolve_obstacle_segments    byte-breaking

            Gate: W3 merged

W4 (Conformance expand) ── docs-only       non-byte-breaking
```

---

## W0 — Prep

**Byte-breaking:** No. Items parallelisable.

### W0-[A] Protocol scaffolding

**Task ref:** #18 (re-label v0.12.0)
**Scope:** Add `resolve_obstacle_boxes` + `resolve_obstacle_segments` stubs
returning `[]` to 11 primitives. `PrimitiveProtocol` extended. No logic.
**Gate out:** stub methods present; `PrimitiveProtocol` type-checks.
**Agent count:** 1.

### W0-[B] Segment-rect helpers

**Task ref:** #16 (partial — re-label v0.12.0)
**Scope:** `_segment_intersects_rect` + `_segment_rect_clip_length` in
`_svg_helpers.py` via Liang–Barsky (reuses `_line_rect_intersection` from R-08).
Used by W1 scoring (P7 term) and W3 segment wiring.
**Gate out:** unit tests pass; no other files touched.
**Agent count:** 1.

### W0-[C] Scoring implementation spec

**Task ref:** #15 (re-label v0.12.0)
**Scope:** This doc + `docs/plans/smart-label-scoring-impl-2026-04-22.md`.
**Gate out:** both docs committed.
**Agent count:** 1 (architect).

### W0-[D] Corpus expand 3 → 44

**Task ref:** #17 (re-label v0.12.0)
**Scope:** 41 new golden fixtures covering all primitive × annotation-type combos.
**Status:** PENDING human sign-off on scene list.
**Dependency note:** STRONGLY RECOMMENDED before W1 calibration but not required
for W1 implementation. W1 can ship un-calibrated; post-W1 patch re-runs grid
search once [D] lands. If delayed, scoring validated on 3 fixtures only —
document limitation in W1 commit message.
**Agent count:** 1.

---

## W1 — Scoring Function

**Byte-breaking:** Yes.
**Gate:** W0-[A,B,C] merged. W0-[D] strongly recommended.

### Scope

Replace first-fit boolean loops at 4 `overlaps()` call sites in
`emit_plain_arrow_svg` (~923, ~935) and `emit_arrow_svg` (~1324, ~1336), plus
`_place_pill` (~1763, ~1782, ~1905, ~1915).

Migration: 4-commit sequence in `smart-label-scoring-impl-2026-04-22.md` §1.3.
Byte-breaking boundary is commits 3 and 4 (or merged 3+4).

**Rules landed:** R-05, R-06, R-10, R-17 (minimum-overlap fallback is special
case of argmin scoring).

**Golden impact:** Atomic re-pin. 44 if [D] landed, else 3. Never partial.

**Post-W1 calibration:** `scripts/tune_label_weights.py` over corpus. Freeze
weights. `test_weights_are_frozen` locks. See scoring impl spec §3.

**Agent count:** 1 (single-file change surface).

---

## W2 — AABB Obstacles

**Byte-breaking:** Yes.
**Gate:** W1 merged.

### Scope

Real geometry in `resolve_obstacle_boxes` per primitive. Wire into
`_nudge_candidates` via W1's `_Obstacle` type.

**Rules landed:** R-02 (target cell MUST), R-04 (source cell SHOULD), R-18
(pre-register AABBs), R-21 (per-candidate viewbox clamp — same viewbox threading
infra), R-20 (`emit_position_label_svg` → `_nudge_candidates`, needs R-21),
R-23 (pill border opacity), R-24 (keyboard a11y), R-28 (loud
`placed_labels=None` warn), R-29 (print `@media`).

**6 AABB-emitting primitives:** `Array`, `Matrix`, `Grid`, `DpTable`, `Graph`,
`Tree`. Others return `[]`.

**Agent count:** 2:
- W2-α: `Array`, `Matrix`, `Grid`, `DpTable` (regular-cell pattern).
- W2-β: `Graph`, `Tree` + non-primitive rules (R-20/21/28/29).

**Golden impact:** Re-pin fixtures for AABB-emitting primitives.

---

## W3 — Segment Obstacles (R-31)

**Byte-breaking:** Yes — `plane2d`, `graph`, `tree`, `numberline`.
**Gate:** W2 merged.

### Scope

Real `resolve_obstacle_segments` on 4 segment-emitting primitives. Wire
`_segment_rect_clip_length` (W0-[B]) into P7 scoring term. Extend `_Obstacle`
with `x2`, `y2` if not present from W1.

**Rule landed:** R-31 (new). Ruleset v2.1.0-rc bump: add R-31 card to
`docs/spec/smart-label-ruleset.md` §8 conformance matrix. See R-31 card text in
`docs/archive/smart-label-edge-avoidance-2026-04-22/R-31-plan.md` §7.

**Segments per primitive:**

| Primitive | Segments |
|-----------|----------|
| `Plane2d` | `p.line[*]`, axis spines, tick marks |
| `Graph` | Edges (decompose polyline curves into segments) |
| `Tree` | Parent-child edges |
| `NumberLine` | Axis spine, tick strokes |

Hard-block: `state="current"` → `severity="MUST"` → `float("inf")`. Dim/done →
`SHOULD` → `W_EDGE_OCCLUSION` term.

**Agent count:** 2:
- W3-α: `Plane2d` + `Graph` (continuous lines, polyline decomposition).
- W3-β: `Tree` + `NumberLine` + ruleset v2.1.0-rc bump.

**Golden impact:** Re-pin all affected fixtures (plane2d/graph/tree/numberline).

---

## W4 — Conformance Matrix Expand

**Byte-breaking:** No.
**Gate:** W3 merged.

**Task ref:** #19 (re-label v0.12.0)
**Scope:** Expand `docs/spec/smart-label-ruleset.md` §8 from 5 tested rules to 34.
Map each W1/W2/W3 rule to test-refs and golden-refs. Status "Gap" → "Shipped".

**Agent count:** 1.

---

## Gate Summary

| Gate | Requires | Unlocks |
|------|----------|---------|
| W0 closed | #15, #16, #18 merged; #17 recommended | W1 start |
| W1 closed | Scoring wired; goldens re-pinned; weights frozen | W2 start |
| W2 closed | AABB obstacles real; 6 primitives wired | W3 start |
| W3 closed | Segment obstacles wired; R-31 card in v2.1.0-rc | W4 start |
| W4 closed | Conformance matrix 34 rules | v0.12.0 RC |

---

## Agent Count Summary

| Wave | Agents | Notes |
|------|--------|-------|
| W0 | 3 (+ 1 human for [D]) | [A], [B], [C] fully parallel; [D] human-gated |
| W1 | 1 | Surgical single-file refactor |
| W2 | 2 | W2-α cells, W2-β graph/tree + non-primitive rules |
| W3 | 2 | W3-α plane2d/graph, W3-β tree/numberline + ruleset |
| W4 | 1 | Docs only |

---

## Task Re-labelling Reference (for main agent)

| ID | Old label | Correct label | Wave |
|----|-----------|---------------|------|
| #15 | v0.11.0 W1 architect plan | v0.12.0 W0-[C] | W0 |
| #16 | v0.11.0 W2-A MW-2 + FP cleanup | v0.12.0 W0-[B] + W2-β | W0 + W2 |
| #17 | v0.11.0 W2-B corpus expand 3→44 | v0.12.0 W0-[D] | W0 |
| #18 | v0.11.0 W2-C Protocol methods 11 primitives | v0.12.0 W0-[A] | W0 |
| #19 | v0.11.0 W2-D conformance expand 5→34 | v0.12.0 W4 | W4 |

---

## Risks

| # | Risk | Mitigation |
|---|------|------------|
| R1 | W0-[D] delayed past W1 | Ship un-calibrated; post-W1 calibration patch |
| R2 | W1 golden drift on 3 fixtures | `test_scoring_reproduces_golden_placements` gate; atomic re-pin |
| R3 | W2-α and W2-β `_svg_helpers.py` conflict | W2-α primitives only; W2-β owns `_svg_helpers.py` exclusively |
| R4 | W3-α polyline decomposition O(N²) | Profile; add bounding-box pre-filter if hotspot |
| R5 | R-31 MUST over-constrains dense scenes | R-19 warning; fallback argmin over non-inf |
| R6 | Ruleset v2.1.0-rc bump races unrelated edits | W3-β owns ruleset file during W3 |

---

## Cross-references

- Scoring impl spec: `docs/plans/smart-label-scoring-impl-2026-04-22.md`
- Scoring proposal: `docs/plans/smart-label-scoring-proposal-2026-04-22.md`
- R-31 plan: `docs/archive/smart-label-edge-avoidance-2026-04-22/R-31-plan.md`
- Ruleset v2.0.0: `docs/spec/smart-label-ruleset.md`
- v2 impl history: `docs/plans/smart-label-v2-impl-plan.md`
- Decision matrix: `docs/archive/smart-label-placement-pedagogy-2026-04-21/00-synthesis.md`
