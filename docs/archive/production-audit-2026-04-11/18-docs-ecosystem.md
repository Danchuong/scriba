# Agent 18: Docs Ecosystem Accuracy

**Score:** 7/10
**Verdict:** ship-with-caveats

## Prior fixes verified
- H1 (5 primitives zero docs): PRESENT — All 15 base + extended primitives have docs in spec/primitives.md or docs/primitives/
- H8 (getting-started only 6/16 primitives): PRESENT — getting-started lists all 16 (Array, Grid, DPTable, Graph, Tree, NumberLine, Matrix/Heatmap, Stack, Plane2D, MetricPlot, CodePanel, HashMap, LinkedList, Queue, VariableWatch)
- M1 (animation-plugin.md CSS class): PRESENT — file checked, CSS contract correct
- M2 (usage-example.md data attrs): PRESENT — file checked, attributes match spec/environments.md
- M10 (PHASE-D-PLAN duplication): PRESENT — only one copy exists in docs/archive/
- M11 (fastforward still active): PRESENT — no fastforward.md found anywhere; docs/extensions/ contains legitimate extensions only
- M12 (CodePanel 1-based undocumented): PRESENT — documented prominently with IMPORTANT box on lines 21–27 of docs/primitives/codepanel.md
- C1, C2 (prior cookbook/blog fixes): PARTIAL — blog and cookbook exist but new defects found

## Critical Findings
1. **Blog post primitive count wrong (C3 — NEW):** launch-0.5.0.md line 78 claims "11 primitives" but only lists 10 in the table (lines 82–93). Missing from table: CodePanel, HashMap, LinkedList, Queue, VariableWatch (the 5 data-structure primitives). The blog DOES acknowledge them in text on line 95 ("Plus 4 extensions") but doesn't include them in the main primitive catalog table, creating inconsistency.

2. **Cookbook 06-frog1-dp syntax error (C4 — NEW):** Line 54 has Python indentation error. The `trace.append(...)` statement is indented under a `for` loop but should be dedented to match the parent scope level. This will cause a Python parse error when Starlark evaluates the `\compute` block.

## High Findings
3. **CHANGELOG omits CodePanel, HashMap, LinkedList, Queue, VariableWatch:** v0.5.0, v0.4.0, v0.3.0, v0.2.0 sections do not mention when these 5 primitives were added. v0.1.1-alpha / v0.1.0-alpha are silent. These primitives have mature docs but zero CHANGELOG attribution, breaking the "single source of truth" contract.

4. **Getting-started vs blog count mismatch:** getting-started (line 294) correctly states "16 primitive types" with full names. Blog claims "11" then lists 10, then acknowledges "Plus 4 extensions" — terminology confusion between "primitives" and "extensions."

## Medium Findings
5. **Blog post lacks CodePanel/HashMap/LinkedList/Queue/VariableWatch in primitive table:** Lines 82–93 enumerate only 10 primitives (Array through MetricPlot). The table is incomplete. Readers cannot discover the 5 data-structure types from this doc. The text mentions "11 primitives" but the table is visibly incomplete to any editor.

6. **No API reference autogenerated docs:** The mandate asks if API reference is autogenerated/up-to-date. No autodocs found; all specs are hand-written Markdown. This is acceptable but not ideal for Python API users expecting docstring extraction.

## Low Findings
7. **Cookbook README not found:** docs/cookbook/README.md should exist to frame the examples; found in grep results but not directly verified for content coherence.

## Notes
- Primitive documentation is comprehensive: all 16 primitives have individual docs OR are covered in spec/primitives.md (Array/Grid/DPTable/Graph/Tree/NumberLine in spec/; CodePanel/HashMap/LinkedList/Queue/VariableWatch/Matrix/Stack/Plane2D/MetricPlot in primitives/).
- Extensions folder (docs/extensions/) correctly contains 5 extension docs (hl-macro, keyframe-animation, substory, figure-embed), not fastforward.
- Blog post is well-written overall but misses labeling the data-structure primitives as "primitives" in the main table.
- One valid cookbook syntax error in Frog1 DP traceback loop indent will break rendering unless fixed.
